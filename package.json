{
  "private": true,
  "scripts": {
    "script": "ts-node --project scripts/tsconfig.json scripts",
    "precodegen": "rimraf src/generated",
    "codegen": "graph codegen subgraph.yaml --output-dir src/generated",
    "postcodegen": "yarn script flatten",
    "create": "graph create melonproject/chainlink-oracle --node http://localhost:8020",
    "predeploy": "yarn create || true",
    "deploy": "graph deploy melonproject/chainlink-oracle subgraph.yaml --ipfs http://localhost:5001 --node http://localhost:8020",
    "format": "prettier --write {src,scripts}/{*,**/*}.{ts,json,yaml}",
    "lint": "yarn prettier --list-different {src,scripts}/{*,**/*}.{ts,json,yaml}"
  },
  "devDependencies": {
    "@chainlink/contracts": "^0.0.8",
    "@graphprotocol/graph-cli": "^0.18.0",
    "@graphprotocol/graph-ts": "^0.18.1",
    "@types/mustache": "^4.0.1",
    "@types/node": "^13.13.5",
    "@types/prettier": "^2.0.0",
    "@types/rimraf": "^3.0.0",
    "@types/yargs": "^15.0.4",
    "assemblyscript": "^0.14.7",
    "glob": "^7.1.6",
    "jest": "^26.2.2",
    "prettier": "^2.0.5",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.1.4",
    "ts-node": "^8.10.1",
    "typescript": "^4.0.0-beta",
    "yargs": "^15.3.1"
  }
}
